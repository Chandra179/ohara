//! Pure text functions (§1.3): language id, domain-dictionary typo correction,
//! tokenizer alignment, Unicode normalization. Zero I/O, no async — the cheapest
//! code to test exhaustively. Implementations arrive with the clean/chunk build
//! steps (§15 steps 3–4).
